---
  - name: create files older then 90 days
    hosts: all
    tasks:
      - name: create files
        file:
          path: /tmp/file{1..5}
          state: touch
          access_time: 201805010923.15
        when: ansible_distribution == "CentOS"
