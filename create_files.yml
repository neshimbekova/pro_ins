---
  - name: create files older then 90 days
    hosts: all
    tasks:
      - name: create files
        file:
          path: /tmp/file{1..5}
          state: touch
          access_time: %2018%05%01%09%23.%15
        when: ansible_distribution == "CentOS"
